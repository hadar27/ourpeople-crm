# Copy to .dev.vars (already gitignored) for local `bun run dev`.
# Service-role secret key from Supabase dashboard → Settings → API Keys.
# Must NEVER be prefixed VITE_ and never committed.
SUPABASE_SERVICE_ROLE_KEY=sb_secret_...
